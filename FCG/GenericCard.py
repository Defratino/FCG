class GenericCard:

    def __init__(self, card):
        self.power = card[0]
        self.symbol = card[1]

    # adds a unit to the next game depends on it stats
    def create_unit(self):
        # names will be changed
        # level.units.add(Unit(self))
        pass
